static DECLARE_DELAYED_WORK(new_work, work_function);

struct workqueue_struct workqueue = create_workqueue("new_workqueue");
INIT_DELAYED_WORK(&new_work, work_function);

queue_delayed_work(workqueue, &new_work, DELAY_TIMEOUT);

cancel_delayed_work(&new_work);
destroy_workqueue(workqueue);


